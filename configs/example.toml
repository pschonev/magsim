# Racer selection
include_racers = ["Banana", "Centaur", "Magician", "Scoocher", "Gunk"]
exclude_racers = []

# Test configurations
racer_counts = [5]
boards = ["Standard", "WildWilds"]

# Execution limits
runs_per_combination = 500
max_total_runs = 10_000
max_turns_per_race = 500
